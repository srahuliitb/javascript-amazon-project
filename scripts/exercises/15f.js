// Exercises
// 15e
export default function isWeekend(date) {
  let result = '';
  if (date === 'Saturday') {
    result = 'Saturday';
  } else if (date === 'Sunday') {
    result = 'Sunday';
  }
  return result;
}
